<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.moose.foodies">

    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".FoodiesApplication"
        android:allowBackup="true"
        android:hardwareAccelerated="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:usesCleartextTraffic="true"
        tools:ignore="AllowBackup">

        <activity android:name=".features.feature_ingredients.IngredientsActivity" />
        <activity android:name=".features.feature_search.SearchActivity" />
        <activity android:name=".features.feature_favorites.FavoritesActivity" />
        <activity android:name=".features.feature_recipe.RecipeActivity" />
        <activity android:name=".features.reset.ResetPasswordActivity" />
        <activity android:name=".features.feature_auth.AuthActivity" />
        <activity android:name=".features.feature_home.HomeActivity" />
        <activity android:name=".features.intolerances.IntolerancesActivity" />
        <activity android:name=".features.feature_splash.SplashActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>